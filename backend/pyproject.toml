[project]
name = "boodschappen-api"
version = "0.1.0"
description = "Groceries list API for Boodschappen app"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

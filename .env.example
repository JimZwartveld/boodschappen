# Boodschappen App Configuration
# Copy this file to .env and customize

# =============================================================================
# Application
# =============================================================================
# Version tag for Docker images (usually set by CI/CD)
APP_VERSION=latest

# =============================================================================
# API Configuration
# =============================================================================
# API token for authentication (optional - leave empty for Tailscale-only access)
# Generate with: openssl rand -hex 32
API_TOKEN=

# Allowed CORS origins (comma-separated, or * for all)
# For Tailscale: http://your-machine-name:80,http://your-machine-name.tailnet-name.ts.net
CORS_ORIGINS=*

# Logging level: debug, info, warning, error
LOG_LEVEL=info

# =============================================================================
# Database
# =============================================================================
# SQLite database path (relative to /app inside container)
# The actual file will be at /app/data/groceries.db
DATABASE_URL=sqlite:///data/groceries.db

# =============================================================================
# Network / Access
# =============================================================================
# Base URL for the API (used for generating links)
# For Tailscale: http://your-machine-name:8000
# For public: https://boodschappen.yourdomain.com
API_BASE_URL=http://localhost:8000

# =============================================================================
# Optional: Cloudflare Tunnel (for public access)
# =============================================================================
# CF_ACCESS_CLIENT_ID=your-client-id
# CF_ACCESS_CLIENT_SECRET=your-client-secret
